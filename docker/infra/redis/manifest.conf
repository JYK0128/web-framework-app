# Redis 기본 설정 파일
# 바인딩 및 포트
bind 0.0.0.0
port 6379

# 백그라운드 저장(영속성 RDB / AOF)
save 900 1
save 300 10
save 60 10000
appendonly yes
appendfsync everysec

# 메모리 관리
maxmemory 512mb
maxmemory-policy allkeys-lru

# 타임아웃
timeout 0
tcp-keepalive 300
