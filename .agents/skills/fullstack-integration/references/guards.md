# Guards

Follow the registration order and application style in `src/common/core.module.ts` and the relevant Guard implementation for authentication, authorization, terms, and verification guards.

Do not declare the same Guard redundantly on individual endpoints.
